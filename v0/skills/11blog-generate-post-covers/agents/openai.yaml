interface:
  display_name: "Generate 11blog post covers"
  short_description: "Generate bundled post covers via 11brands"
  default_prompt: "Use blog-generate-post-covers to generate one cohesive batch of versioned post covers."
