interface:
  display_name: "Generate publication covers"
  short_description: "Generate 11blog covers through 11brands"
  default_prompt: "Use $11blog-generate-publication-covers to generate and import publication cover assets."
